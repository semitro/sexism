import React from 'react';
import PropTypes from 'prop-types';
import {Panel, List, Button, Group, Div, Avatar, PanelHeader, Alert, Epic} from '@vkontakte/vkui';

const Vagina = ({id}) => (
    <Panel id={id}>
        <Alert>
        <h2>ВАМ ЗАПРЕЩЁН ДОСТУП К ПРИЛОЖЕНИЮ</h2>
        </Alert>
    </Panel>
);


export default Vagina;
